
  // Meu codigo 

  /*  let result = Math.floor( Math.random() * 5);
    console.log(result);
  
    number = 2;

   if (number === (result)) {
    console.log('Parabéns você ganhou');
   } else console.log('Tente novamente');
   

   // Codigo do Gabarito

   const numberChecker = (myNumber, number) => myNumber === number;
const lotteryResult = (myNumber, callback) => {
  const num + 1);
  return callback(myNumber, number) ? 'Lucky dber = Math.floor((Math.random() * 5)ay, you won!' : 'Try Again!';
};
console.log(lotteryResult(2, numberChecker));*/

//Amanha ve como resolve o primeiro exercicio com o video do gabarito, consegui resolver o segundo exercicio mas não ficou como esperado reveja isso também.

const email = '@gmail.com'

const id1 = () => `Pedro_Guerra${email}`
const id2 = () => `Luiza_Drumond${email}`
const id3 = () => `Carla_Paiva${email}`

const newEmployees = (callback) => {
  const result = callback();
  console.log(result);
}

newEmployees(id1)
newEmployees(id2);
newEmployees(id3);